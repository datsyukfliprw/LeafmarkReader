import 'dotenv/config'; import Database from 'better-sqlite3'; import fs from 'node:fs'; import path from 'node:path';
const source=path.resolve(process.env.DATABASE_PATH??'./data/leafmark.sqlite');const dir=path.resolve(process.env.BACKUP_DIR??'./backups');fs.mkdirSync(dir,{recursive:true});if(!fs.existsSync(source))throw new Error(`Database not found: ${source}`);const stamp=new Date().toISOString().replace(/[:.]/g,'-');const dest=path.join(dir,`leafmark-${stamp}.sqlite`);const db=new Database(source,{readonly:true,fileMustExist:true});await db.backup(dest);db.close();const check=new Database(dest,{readonly:true});const result=check.pragma('integrity_check',{simple:true});check.close();if(result!=='ok'){fs.unlinkSync(dest);throw new Error(`Backup integrity check failed: ${result}`)}console.log(dest);
