import 'dotenv/config'; import Database from 'better-sqlite3'; import fs from 'node:fs'; import path from 'node:path';
const input=process.env.RESTORE_FROM;if(!input)throw new Error('Set RESTORE_FROM=/path/to/leafmark-backup.sqlite');const source=path.resolve(input);const target=path.resolve(process.env.DATABASE_PATH??'./data/leafmark.sqlite');if(!fs.existsSync(source))throw new Error(`Backup not found: ${source}`);const check=new Database(source,{readonly:true,fileMustExist:true});const result=check.pragma('integrity_check',{simple:true});check.close();if(result!=='ok')throw new Error(`Backup failed integrity check: ${result}`);fs.mkdirSync(path.dirname(target),{recursive:true});if(fs.existsSync(target))fs.copyFileSync(target,`${target}.pre-restore-${Date.now()}`);for(const suffix of ['','-wal','-shm'])try{fs.unlinkSync(target+suffix)}catch{}fs.copyFileSync(source,target);console.log(`Restored ${source} -> ${target}`);
