import { describe,it,expect } from 'vitest'; import { deterministicQuestion,evidenceRegistry } from '@leafmark/pedagogy';
describe('hard pedagogy rules',()=>{it('questions contain no canonical book facts',()=>{for(const e of evidenceRegistry.filter(x=>x.id.startsWith('READ.'))){const q=deterministicQuestion(e.skill as any,1).question;expect(q.length).toBeGreaterThan(8);expect(q).not.toMatch(/the answer|correct answer/i)}});it('every reading strategy prohibits answer leakage',()=>{for(const e of evidenceRegistry.filter(x=>x.id.startsWith('READ.')))expect(e.prohibitedAiBehavior.join(' ')).toMatch(/Reveal an answer/)})});
