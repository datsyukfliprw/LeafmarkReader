import { test,expect } from '@playwright/test';
test('child can sign in and sees the simple three-destination shell',async({page})=>{await page.goto('/');await expect(page.getByRole('heading',{name:"Who’s reading?"})).toBeVisible();await page.getByRole('button',{name:/Gavin/}).click();await page.getByLabel('Reading PIN').fill('1357');await page.getByRole('button',{name:'Open Leafmark'}).click();await expect(page.getByRole('link',{name:/Today/})).toBeVisible();await expect(page.getByRole('link',{name:/My Books/})).toBeVisible();await expect(page.getByRole('link',{name:/My Journey/})).toBeVisible()});
