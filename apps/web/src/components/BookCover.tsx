import { useState } from 'react'; import { BookOpen } from 'lucide-react';
export function BookCover({src,title,author,size='large'}:{src?:string|null;title:string;author?:string|null;size?:'small'|'medium'|'large'}){const [failed,setFailed]=useState(false);const showImage=Boolean(src)&&!failed;return <div className={`book-cover book-cover--${size}`} aria-label={`Cover of ${title}`}>{showImage?<img src={src!} alt="" loading="lazy" onError={()=>setFailed(true)}/>:<div className="book-cover__fallback"><BookOpen aria-hidden="true"/><strong>{title}</strong><span>{author}</span></div>}<span className="book-cover__sheen"/></div>}
