import { afterEach,describe,expect,it,vi } from 'vitest'; import { OpenLibraryProvider } from './metadata';
afterEach(()=>vi.unstubAllGlobals());
describe('OpenLibraryProvider',()=>{it('normalizes reliable metadata without inventing missing fields',async()=>{vi.stubGlobal('fetch',vi.fn(async()=>new Response(JSON.stringify({'ISBN:9780306406157':{title:'Test Book',authors:[{name:'A. Author'}],number_of_pages:123,cover:{large:'https://example.com/cover.jpg'}}}),{status:200})));const r=await new OpenLibraryProvider(100).lookup('978-0-306-40615-7');expect(r).toMatchObject({isbn:'9780306406157',title:'Test Book',author:'A. Author',pageCount:123,source:'Open Library'});expect(r?.publisher).toBeNull()});it('rejects invalid ISBN locally before network access',async()=>{const f=vi.fn();vi.stubGlobal('fetch',f);await expect(new OpenLibraryProvider().lookup('123')).rejects.toThrow('invalid_isbn');expect(f).not.toHaveBeenCalled()})});
